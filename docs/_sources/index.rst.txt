.. RM-Gen documentation master file, created by
   sphinx-quickstart on Mon Dec  8 18:27:21 2025.
   You can adapt this file completely to your liking, but it should at least
   contain the root `toctree` directive.

RM-Gen documentation
====================

This package provides extensible code for generating radiomap datasets for AI training. It includes utilities needed
for pipeline definition, feature generation and raytracing simulation to generate radiomap ground truth.

For now the code only supports path loss map generation using sionna.

.. Add your content using ``reStructuredText`` syntax. See the
.. `reStructuredText <https://www.sphinx-doc.org/en/master/usage/restructuredtext/index.html>`_
.. documentation for details.

Checkout how to install the library on the `installation <installation>`_ page, and see some example pipelines on the
`examples <examples>`_ page.

`API reference <api/modules>`_ provides detailed information about all modules and classes provided by the library.

.. toctree::
   :maxdepth: 4

   installation
   examples
   api/modules
